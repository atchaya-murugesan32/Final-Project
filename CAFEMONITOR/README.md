# CAFEMONITOR

A FastAPI-based backend for monitoring cafe busyness and recommendations.

## Run locally

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload
```
